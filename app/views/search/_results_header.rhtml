<div class="results-header">
  <%= label_total_found(asset, results.total_entries) %>
  <span class="current-page"><%= _("Showing page %s of %s") % [results.current_page, results.total_pages] %></div>
  <%= facets_unselect_menu(asset) %>
  <%= order_by(asset) %>
</div>
