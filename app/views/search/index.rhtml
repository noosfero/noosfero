<h2> <%= _('Listing articles found')  %> </h2>

<h3> <%= _('Searching for ') + @query  %></h3>
<% @results.each do |a| %>
  <h4> <%= link_to_document(a) if a.kind_of?(Article) %> </h4>
  <h4> <%= link_to_homepage(a.name, a.identifier) if a.kind_of?(Profile) %> </h4>

<% end %>
