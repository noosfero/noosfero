<h1> <%=h @category ? (_('Search results for "%{query}" in "%{category}"') % { :query => @query, :category => @category.name}) : (_('Search results for "%s"') % @query) %> </h1>

<%= render :partial => 'display_results' %>

<%= render :partial => 'search_form', :locals => { :form_title => _("Filter your search") } %>
<br style="clear:both" />

