<h1>
  <%= _("%s's events") % @environment.name %>
  <% if @category %>
    :<small><%= @category.name %></small>
  <% end %>
</h1>

<% stylesheet 'controller_events' %>

<%= render :partial => 'events/agenda' %>
