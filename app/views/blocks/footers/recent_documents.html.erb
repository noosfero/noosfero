<% if block.owner.is_a?(Profile) %>
    <%= render partial: 'blocks/footers/view_all', locals: {:profile => block.owner.identifier, :controller => 'profile', :action => 'sitemap', :block_elements => block.docs} %>
<% end %>
