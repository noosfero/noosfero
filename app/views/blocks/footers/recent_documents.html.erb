<% if !block.docs.empty? %>
  <% if block.owner.is_a?(Profile) %>
    <%= link_to _('All content'), {:profile => block.owner.identifier, :controller => 'profile', :action => 'sitemap'},  :class => "view-all" %>
  <% end %>
<% end %>
