<% if block.owner.is_a?(Environment) %>
  <%= link_to s_('tags|View all'), :controller => 'search', :action => 'tags' %>
<% else %>
  <%= link_to s_('tags|View all'), :profile => block.owner.identifier, :controller => 'profile', :action => 'tags' %>
<% end %>
