<ul>
  <%if logged_in? && (user != profile) && (! user.friends.include?(profile)) %>
    <li><%= link_to _('Add friend'), :profile => user.identifier, :controller => 'friends', :action => 'add', :id => profile.id %></li>
  <% end %>
</ul>
