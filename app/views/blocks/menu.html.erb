<%= block_title(block.title, block.subtitle) %>

<ul>
  <li> <%= link_to _('Activities'), {:controller => 'profile', :action => 'activities', :profile => block.owner.identifier} %></li>
  <% if profile.person? %>
    <li> <%= link_to _('About'), {:controller => 'profile', :action => 'about', :profile => block.owner.identifier} %></li>
    <li> <%= link_to _('Communities'), {:controller => 'memberships', :action => 'index', :profile => block.owner.identifier} %></li>
    <li> <%= link_to _('People'), {:controller => 'friends', :action => 'index', :profile => block.owner.identifier} %></li>
  <% end %>
  <% if profile.community? %>
    <li> <%= link_to _('People'), {:controller => 'profile_members', :action => 'index', :profile => block.owner.identifier} %></li>
  <% end %>
</ul>

