= content_for :head do
  = javascript_include_tag 'vendor/typeahead.bundle.js'
  = stylesheet_link_tag 'vendor/typeahead'

= text_field_tag :query, nil, id: 'basket-add', placeholder: t('suppliers_plugin.views.manage_products.basket_tab.search')

