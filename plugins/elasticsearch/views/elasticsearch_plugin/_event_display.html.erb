<div class="event-header">
  <%= show_time(event.created_at) %> - <span class="model-label"><%= model_label.singularize %></span>
</div>

<div class="body">
   <h2><%= link_to event.name, event.url %></h2>
   <p><%= event.body %></p>
</div>
