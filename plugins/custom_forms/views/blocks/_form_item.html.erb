<%= link_to({ controller: 'custom_forms_plugin_profile', action: 'show',
              id: form.id }, { class: 'form-item' }) do %>

  <div class='form-item-img'>
    <% if form.begining.present? || form.ending.present? %>
      <span class='period-overlay'><%= next_period(form) %></span>
    <% end %>
    <%= form_image_tag(form) %>
    </span>
  </div>

  <div class='form-item-info'>
    <div class='form-item-header'>
      <span class='form-name'><%= form.name %></span>
      <span class='form-status <%= form.status.to_slug %>'>
        <%= _(form.status) %>
      </span>
    </div>
    <div class='form-submissions'>
      <%= _("%s submissions") % form.submissions.count %>
    </div>
  </div>

<% end %>
